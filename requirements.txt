# torch is NOT listed: it comes from the vast.ai image (/venv/main) or the
# local conda env. Reinstalling it on-instance wastes 10+ minutes.
numpy
wandb
matplotlib
# data path: parquet shards pulled once from the hub and cached as local
# bytes (scripts/m5_data.py). Replaces `datasets` streaming, which was flaky
# mid-run; both of these used to arrive as datasets' own dependencies.
huggingface_hub
pyarrow
# the GPT-NeoX tokenizer that builds the uint16 token cache, and the
# remote-code loader score_ref.py uses to pull open-sci-ref's own modelling
# file. Not needed by the byte path or by bench_batch.py (synthetic).
transformers
